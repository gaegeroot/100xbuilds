<!DOCTYPE html>
<html lang="en">
    <head>
        <meta
        charset="utf-8"/>
        {# Dynamic SEO Title - Use page title if available, otherwise site default #}
        {% if title %}
            <title>{{ title }} | 100XBuilds</title>
        {% else %}
            <title>100XBuilds - Marketing & Sales Automation for Luxury Custom Home Builders</title>
        {% endif %}
        {# Dynamic Meta Description - Use page description if available #}
        {% if des %}
            <meta name="description" content="{{ des }}">
        {% else %}
            <meta
                name="description"
                content="100XBuilds provides marketing automation, sales systems, and business operations solutions for luxury custom
                    home builders with $5M+ annual revenue. Guaranteed ROI improvements.">
        {% endif %}
        
        <meta name="generator" content="Eleventy">
        <meta name="robots" content="noindex, Nofollow, Noimageindex">
        <meta name="viewport" content="width=device-width, initial-scale=1"/>
        <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1"/>
        <link
        rel="stylesheet" href="{{ '/assets/vendor/font-awesome/css/font-awesome.min.css' | url }}"/> <!-- Bootstrap -->
        <link
        rel="stylesheet" href="{{ '/assets/vendor/bootstrap/bootstrap.min.css' | url }}"/> <!-- Stylesheets -->
        <link href="{{ '/assets/css/style.css' | url }}" rel="stylesheet"/>
        <link rel="shortcut icon" href="{{ '/assets/images/favicon.png' | url }}" type="image/x-icon"/>
        <link rel="icon" href="{{ '/assets/images/favicon.png' | url }}" type="image/x-icon"/>
        {% if excludeTracking != true %}
            <!-- Facebook Pixel -->
            <!-- Google Analytics --><script async src="https://www.googletagmanager.com/gtag/js?id=GA_MEASUREMENT_ID"> </script>
            <script>
                window.dataLayer = window.dataLayer || [];
                function gtag() {
                    dataLayer.push(arguments);
                }
                gtag('js', new Date());
                gtag('config', 'GA_MEASUREMENT_ID');
            </script>
        {% endif %}
    </head>
    <body>
        {% block navbar %}
            <nav class="navbar navbar-expand-lg site-navigation nav-bg">
                <div class="container">
                    <a class="navbar-brand" href="{{ '/' | url }}">
                        <img src="{{ nav.navLogo | url }}" alt="nav-logo"/>
                    </a>
                    <div class="d-flex align-items-center">
                        <a class="nav-link btn btn-sm btn-primary btn-sm-rounded text-light d-lg-none me-2" href="{{ ('/' + nav.navBtnLink) | url }}">
                            <span class="btn-area">
                                <span data-text="{{ nav.navBtnText }}">
                                    {{ nav.navBtnText }}
                                </span>
                            </span>
                        </a>
                        <button
                            class="navbar-toggler collapsed"
                            type="button"
                            data-bs-toggle="collapse"
                            data-bs-target="#sitenavbar"
                            aria-controls="sitenavbar"
                            aria-expanded="false"
                            aria-label="Toggle navigation">
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                            <span class="icon-bar"></span>
                        </button>
                    </div>
                    <div class="collapse navbar-collapse" id="sitenavbar">
                        <ul class="navbar-nav ms-auto main-nav">
                            {% for navLink in nav.navLinks %}
                                <li class="nav-item">
                                    {% if navLink == "home" %}
                                        <a class="nav-link" href="{{ '/' | url }}">{{ navLink | capitalize }}</a>
                                    {% else %}
                                        <a class="nav-link" href="{{ ('/' + navLink) | url }}">{{ navLink | capitalize }}</a>
                                    {% endif %}
                                </li>
                            {% endfor %}
                            <li class="d-none d-lg-block nav-item">
                                <a class="nav-link btn btn-sm btn-primary btn-sm-rounded text-light" href="{{ ('/' + nav.navBtnLink) | url }}">
                                    <span class="btn-area">
                                        <span data-text="{{ nav.navBtnText }}">
                                            {{ nav.navBtnText }}
                                        </span>
                                    </span>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
        {% endblock %}
        {% block content %}
            {{ content | safe }}
        {% endblock %}
        {% block workTogether %}
            <section class="site-cta">
                <div class="container">
                    <div class="row">
                        <div class="col-12 text-center">
                            <h1 class="site-cta-title">{{ workTogether.heading }}</h1>
                            <ul class="site-cta-buttons">
                                <li>
                                    <a href="{{ ('/' + workTogether.submitBtn.btnLink) | url }}" class="btn btn-secondary">
                                        <span class="btn-area">
                                            <span data-text="{{ workTogether.submitBtn.btnText }}">
                                                {{ workTogether.submitBtn.btnText }}
                                            </span>
                                        </span>
                                    </a>
                                </li>
                                <li>
                                    <a href="{{ ('/' + workTogether.convBtn.btnLink) | url }}" class="btn btn-primary">
                                        <span class="btn-area">
                                            <span data-text="{{ workTogether.convBtn.btnText }}">
                                                {{ workTogether.convBtn.btnText }}
                                            </span>
                                        </span>
                                    </a>
                                </li>
                            </ul>
                        </div>
                    </div>
                </div>
            </section>
        {% endblock %}
        {% block footer %}
            <footer class="site-footer">
                <div class="container">
                    <div
                        class="row">
                        {# <div class="col-12"></div> #}
                        <div class="col-lg-3 col-md-6">
                            <div class="site-footer-widget">
                                <div class="site-footer-logo">
                                    <a href="{{ '/' | url }}">
                                        <img src="{{ footer.footerLogo | url }}" alt="logo-footer" width="100">
                                    </a>
                                </div>
                                <p
                                    class="site-footer-widget-description">{# {% for contactInfo in footer.contactsInfo %}
                                                                                                                        <a href="{{ contactInfo.link }}">{{ contactInfo.text }}</a>
                                                                                                                        <br>
                                                                                                                    {% endfor %} #}
                                </p>
                            </div>
                        </div>
                        <div class="col-lg-2 col-md-6">
                            <div class="site-footer-widget">
                                <h5 class="site-footer-widget-title">{{ footer.siteMapHeading }}</h5>
                                <ul class="site-footer-widget-links">
                                    {% for siteMap in footer.siteMap %}
                                        <li>
                                            <a href="{{ ('/' + siteMap.link) | url }}">{{ siteMap.text }}</a>
                                        </li>
                                    {% endfor %}
                                </ul>
                            </div>
                        </div>
                        <div class="col-lg-2 col-md-6">
                            <div class="site-footer-widget">
                                <h5 class="site-footer-widget-title">{{ footer.socialHeading }}</h5>
                                <ul class="site-footer-widget-links">
                                    {% for socialMedia in footer.socialMedia %}
                                        <li>
                                            <a href="{{ ('/' + socialMedia.link) | url }}">{{ socialMedia.text }}</a>
                                        </li>
                                    {% endfor %}
                                </ul>
                            </div>
                        </div>
                        <div class="col-lg-3 col-md-6">
                            <div class="site-footer-widget">
                                <h5 class="site-footer-widget-title">{{ footer.brandHeading }}</h5>
                                <p class="site-footer-widget-description">
                                    {% for brandMotto in footer.brandMotto %}
                                        - {{ brandMotto }} <br>
                                    {% endfor %}
                                </p>
                            </div>
                        </div>
                        <div class="col-lg-2 col-12">
                            <a href="#top" class="site-footer-widget-top">
                                <img src="{{ footer.back2topIcon | url }}" alt="back-to-top">
                                <p>
                                    {{ footer.customerComment | safe }}
                                </p>
                            </a>
                        </div>
                        <div class="col-12">
                            <div class="site-footer-copyright text-center my-3">
                                <p>© Copyright 2025 - All Rights Reserved by
                                    <a href="https://100xBuilds.com/" target="_blank">100XBuilds</a>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </footer>
        {% endblock %}
        <!-- <script src="js/vendor.js"></script> -->
        <script src="{{ '/assets/vendor/bootstrap/bootstrap.min.js' | url }}"></script>
        <script src="{{ '/assets/js/script.js' | url }}"></script>
    </body>
</html>